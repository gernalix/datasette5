- tabella di default
  id:: 69091935-1ba3-4c02-a5f6-cc45d1bf7c6f
	- 2 colonne di default: "id" (integer primary key) in posizione 1 e "logseq" (text) in posizione 2, e a seguire tutte le colonne definite dall'utente
	  id:: 69091935-acbf-44fe-aba1-a096f86ad765
	- tutti i trigger in triggers.sql (riadattando ogni trigger con nome e colonne del caso)
- lo script deve lavorare sul db "noutput.db" e "triggers.sql" nella stessa cartella
- MENU aggiungi tabella
	- chiamerò questa tabella <main> in queste istruzioni, per farti capire quando mi riferisco ad essa
	- INPUT chiede: nome tabella,
	- INVIO
	- INPUT: colonna1, colonna2 + opzioni i/r/t (tipo) + z (default = 0)
		- spiegazione: l'input è la lista di colonne separate da virgola. Ogni colonna è seguita da una o due lettere attaccate di cui
			- la prima obbligatoria indica il tipo della colonna ("i" per integer, "r" per real, "t" per text)
			- la seconda facoltativa, "z" indica che il default di quella colonna deve essere uno 0
			- esempio lista: inizio t, fine t, esempio iz, esempio2 iz, esempio3 i
				- questo input deve creare le colonne "inizio" text, "fine" text, "esempio" integer default 0, "esempio2" integer.
			- le colonna andranno create nell'ordine dato dalla lista
	- per ogni colonna che finisce in "_id", crea una foreign key da  "<main>.<fk>_id" alla colonna <fk>.id.
		- Se la tabella <fk> non esiste, crea prima <fk> applicando ((69091935-1ba3-4c02-a5f6-cc45d1bf7c6f)) e poi <main>
	- INVIO
	- applica ((69091935-1ba3-4c02-a5f6-cc45d1bf7c6f))
	- crea tabella